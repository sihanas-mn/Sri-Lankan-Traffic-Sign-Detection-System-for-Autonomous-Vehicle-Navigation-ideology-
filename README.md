# Sri-Lankan-Traffic-Sign-Detection-System-for-Autonomous-Vehicle-Navigation-ideology-
this repo contains the pre-trained YOLOV8 model (trained with secondary data collected in Sri Lanka from the local area on top of the base model) and the Python code to run it on any machine (ready to use).
